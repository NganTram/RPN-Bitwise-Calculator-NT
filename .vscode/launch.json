{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run RPN (g++)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/rpn",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "externalConsole": false,
      "MIMode": "lldb",
      "preLaunchTask": "build rpn (g++)"
    }
  ]
}
